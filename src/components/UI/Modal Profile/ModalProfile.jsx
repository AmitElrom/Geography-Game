import React from "react";

const ModalProfile = ({ button, onClick }) => {
  return (
    <div>
      ModalProfile
      <button onClick={onClick}>{button}</button>
    </div>
  );
};

export default ModalProfile;
